//using Photon.Realtime;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class RoomListItem : MonoBehaviour
{
    /*
    [SerializeField] TMP_Text text;

    RoomInfo info;
    
    
    public void SetUp(Roominfo _info)
    {
        info = _info;
        text.text = _info.Name;
    }

    public void OnClick()
    {

    }

    // We'll need a function to get the text
    // We'll want text to be roomName + "\t (" + currentPlayers + "/" + maxPlayers ")"
    // For example, it might read "Room 4   (1/4)"

    */
}
